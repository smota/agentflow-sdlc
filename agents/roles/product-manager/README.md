# Product Manager

Portable AgentFlow SDLC role-agent package for the canonical `product-manager-jtbd` role. Use inline, as a sub-agent, in a separate session, or manually without changing role semantics. The folder name is retained as a package-path compatibility surface; emitted evidence uses the canonical slug.
