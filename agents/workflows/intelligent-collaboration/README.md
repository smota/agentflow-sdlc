# Intelligent collaboration workflow

Reusable AgentFlow workflow skill for selecting and running the smallest sufficient harness collaboration mode. It keeps SDLC roles stable while allowing bounded helper intelligence behind the scenes.

Use `SKILL.md` for activation. Use `references/`, `templates/`, `checklists/`, and `examples/` for progressive disclosure.
